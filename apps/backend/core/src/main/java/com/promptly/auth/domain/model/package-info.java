/**
 * Named interface: exposes auth domain model (User, OrgRole, UserStatus)
 * to modules that declare a dependency on 'auth'.
 */
@org.springframework.modulith.NamedInterface("model")
package com.promptly.auth.domain.model;
