{
  "name": "promptly-core",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/backend/core/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn clean package -DskipTests -f apps/backend/core/pom.xml"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn spring-boot:run -f apps/backend/core/pom.xml -Dspring-boot.run.profiles=dev"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn test -f apps/backend/core/pom.xml"
      }
    }
  }
}
