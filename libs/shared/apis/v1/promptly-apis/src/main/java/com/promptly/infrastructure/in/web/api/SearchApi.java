/*
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech) (7.21.0).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package com.promptly.infrastructure.in.web.api;

import com.promptly.infrastructure.in.web.dto.DuplicateCheckResponse;
import com.promptly.infrastructure.in.web.dto.SearchResponse;
import io.swagger.v3.oas.annotations.ExternalDocumentation;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.Parameters;
import io.swagger.v3.oas.annotations.media.ArraySchema;
import io.swagger.v3.oas.annotations.media.Content;
import io.swagger.v3.oas.annotations.media.Schema;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.security.SecurityRequirement;
import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.enums.ParameterIn;
import io.swagger.v3.oas.annotations.media.ExampleObject;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Flux;
import reactor.core.publisher.Mono;
import org.springframework.http.codec.multipart.Part;

import jakarta.validation.Valid;
import jakarta.validation.constraints.*;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import jakarta.annotation.Generated;

@Generated(value = "org.openapitools.codegen.languages.SpringCodegen", date = "2026-04-29T16:30:34.655679900-05:00[America/Chicago]", comments = "Generator version: 7.21.0")
@Validated
@Tag(name = "Search", description = "Semantic Search — embedding-based prompt discovery")
public interface SearchApi {

    String PATH_CHECK_DUPLICATES = "/api/v1/prompts/{promptId}/check-duplicates";
    /**
     * POST /api/v1/prompts/{promptId}/check-duplicates : Check for duplicate prompts
     * Identifies potential duplicates based on content similarity (threshold ≥ 0.9).
     *
     * @param promptId Prompt identifier (required)
     * @return Duplicate check result (status code 200)
     */
    @Operation(
        operationId = "checkDuplicates",
        summary = "Check for duplicate prompts",
        description = "Identifies potential duplicates based on content similarity (threshold ≥ 0.9).",
        tags = { "Search" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Duplicate check result", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = DuplicateCheckResponse.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.POST,
        value = SearchApi.PATH_CHECK_DUPLICATES,
        produces = { "application/json" }
    )
    Mono<ResponseEntity<DuplicateCheckResponse>> checkDuplicates(
        @Parameter(name = "promptId", description = "Prompt identifier", required = true, in = ParameterIn.PATH) @PathVariable("promptId") String promptId,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_FIND_SIMILAR_PROMPTS = "/api/v1/prompts/{promptId}/similar";
    /**
     * GET /api/v1/prompts/{promptId}/similar : Find similar prompts
     * Finds prompts with similar content using vector similarity.
     *
     * @param promptId Prompt identifier (required)
     * @return Similar prompts (status code 200)
     */
    @Operation(
        operationId = "findSimilarPrompts",
        summary = "Find similar prompts",
        description = "Finds prompts with similar content using vector similarity.",
        tags = { "Search" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Similar prompts", content = {
                @Content(mediaType = "application/json", array = @ArraySchema(schema = @Schema(implementation = SearchResponse.class)))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.GET,
        value = SearchApi.PATH_FIND_SIMILAR_PROMPTS,
        produces = { "application/json" }
    )
    Mono<ResponseEntity<Flux<SearchResponse>>> findSimilarPrompts(
        @Parameter(name = "promptId", description = "Prompt identifier", required = true, in = ParameterIn.PATH) @PathVariable("promptId") String promptId,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_SEARCH_PROMPTS = "/api/v1/search";
    /**
     * GET /api/v1/search : Semantic search for prompts
     * Uses embedding-based vector search to find prompts similar to the query text.
     *
     * @param q Search query text (required)
     * @return Search results with relevance scores (status code 200)
     */
    @Operation(
        operationId = "searchPrompts",
        summary = "Semantic search for prompts",
        description = "Uses embedding-based vector search to find prompts similar to the query text.",
        tags = { "Search", "PromptlyQuery" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Search results with relevance scores", content = {
                @Content(mediaType = "application/json", array = @ArraySchema(schema = @Schema(implementation = SearchResponse.class)))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.GET,
        value = SearchApi.PATH_SEARCH_PROMPTS,
        produces = { "application/json" }
    )
    Mono<ResponseEntity<Flux<SearchResponse>>> searchPrompts(
        @NotNull @Parameter(name = "q", description = "Search query text", required = true, in = ParameterIn.QUERY) @Valid @RequestParam(value = "q", required = true) String q,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );

}
