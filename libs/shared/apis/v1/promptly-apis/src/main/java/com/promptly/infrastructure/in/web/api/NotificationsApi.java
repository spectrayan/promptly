/*
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech) (7.21.0).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package com.promptly.infrastructure.in.web.api;

import org.springframework.format.annotation.DateTimeFormat;
import com.promptly.infrastructure.in.web.dto.NotificationCountResponse;
import com.promptly.infrastructure.in.web.dto.NotificationListResponse;
import com.promptly.infrastructure.in.web.dto.NotificationPreferenceResponse;
import org.jspecify.annotations.Nullable;
import java.time.OffsetDateTime;
import com.promptly.infrastructure.in.web.dto.ProblemDetails;
import com.promptly.infrastructure.in.web.dto.ProjectNotificationSettingsResponse;
import com.promptly.infrastructure.in.web.dto.UpdateNotificationPreferenceRequest;
import com.promptly.infrastructure.in.web.dto.UpdateProjectNotificationSettingsRequest;
import io.swagger.v3.oas.annotations.ExternalDocumentation;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.Parameters;
import io.swagger.v3.oas.annotations.media.ArraySchema;
import io.swagger.v3.oas.annotations.media.Content;
import io.swagger.v3.oas.annotations.media.Schema;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.security.SecurityRequirement;
import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.enums.ParameterIn;
import io.swagger.v3.oas.annotations.media.ExampleObject;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Flux;
import reactor.core.publisher.Mono;
import org.springframework.http.codec.multipart.Part;

import jakarta.validation.Valid;
import jakarta.validation.constraints.*;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import jakarta.annotation.Generated;

@Generated(value = "org.openapitools.codegen.languages.SpringCodegen", date = "2026-04-29T16:30:34.655679900-05:00[America/Chicago]", comments = "Generator version: 7.21.0")
@Validated
@Tag(name = "Notifications", description = "Notifications — real-time SSE alerts, preferences, and project settings")
public interface NotificationsApi {

    String PATH_CLEAR_ALL_NOTIFICATIONS = "/api/v1/notifications";
    /**
     * DELETE /api/v1/notifications : Clear all notifications
     * Deletes all notifications for the authenticated user in the specified project. 
     *
     * @param projectId Project scope (required)
     * @return All notifications cleared (status code 204)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "clearAllNotifications",
        summary = "Clear all notifications",
        description = "Deletes all notifications for the authenticated user in the specified project. ",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "204", description = "All notifications cleared"),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.DELETE,
        value = NotificationsApi.PATH_CLEAR_ALL_NOTIFICATIONS,
        produces = { "application/problem+json" }
    )
    Mono<ResponseEntity<Void>> clearAllNotifications(
        @NotNull @Parameter(name = "projectId", description = "Project scope", required = true, in = ParameterIn.QUERY) @Valid @RequestParam(value = "projectId", required = true) String projectId,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_DISMISS_NOTIFICATION = "/api/v1/notifications/{id}/read";
    /**
     * DELETE /api/v1/notifications/{id}/read : Dismiss a notification
     * Permanently deletes a single notification for the authenticated user.
     *
     * @param id Notification ID (required)
     * @return Notification dismissed (status code 204)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     *         or The requested resource was not found (status code 404)
     */
    @Operation(
        operationId = "dismissNotification",
        summary = "Dismiss a notification",
        description = "Permanently deletes a single notification for the authenticated user.",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "204", description = "Notification dismissed"),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            }),
            @ApiResponse(responseCode = "404", description = "The requested resource was not found", content = {
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.DELETE,
        value = NotificationsApi.PATH_DISMISS_NOTIFICATION,
        produces = { "application/problem+json" }
    )
    Mono<ResponseEntity<Void>> dismissNotification(
        @Parameter(name = "id", description = "Notification ID", required = true, in = ParameterIn.PATH) @PathVariable("id") String id,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_GET_NOTIFICATION_PREFERENCES = "/api/v1/notifications/preferences";
    /**
     * GET /api/v1/notifications/preferences : Get notification preferences
     * Returns the current user&#39;s notification preferences for a project.
     *
     * @param projectId Project scope (required)
     * @return User notification preferences (status code 200)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "getNotificationPreferences",
        summary = "Get notification preferences",
        description = "Returns the current user's notification preferences for a project.",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "200", description = "User notification preferences", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = NotificationPreferenceResponse.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = NotificationPreferenceResponse.class))
            }),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProblemDetails.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.GET,
        value = NotificationsApi.PATH_GET_NOTIFICATION_PREFERENCES,
        produces = { "application/json", "application/problem+json" }
    )
    Mono<ResponseEntity<NotificationPreferenceResponse>> getNotificationPreferences(
        @NotNull @Parameter(name = "projectId", description = "Project scope", required = true, in = ParameterIn.QUERY) @Valid @RequestParam(value = "projectId", required = true) String projectId,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_GET_PROJECT_NOTIFICATION_SETTINGS = "/api/v1/notifications/project-settings";
    /**
     * GET /api/v1/notifications/project-settings : Get project notification settings
     * Returns project-level notification settings (which event types are enabled).
     *
     * @param projectId Project ID (required)
     * @return Project notification settings (status code 200)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "getProjectNotificationSettings",
        summary = "Get project notification settings",
        description = "Returns project-level notification settings (which event types are enabled).",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Project notification settings", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProjectNotificationSettingsResponse.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProjectNotificationSettingsResponse.class))
            }),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProblemDetails.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.GET,
        value = NotificationsApi.PATH_GET_PROJECT_NOTIFICATION_SETTINGS,
        produces = { "application/json", "application/problem+json" }
    )
    Mono<ResponseEntity<ProjectNotificationSettingsResponse>> getProjectNotificationSettings(
        @NotNull @Parameter(name = "projectId", description = "Project ID", required = true, in = ParameterIn.QUERY) @Valid @RequestParam(value = "projectId", required = true) String projectId,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_GET_UNREAD_NOTIFICATION_COUNT = "/api/v1/notifications/count";
    /**
     * GET /api/v1/notifications/count : Get unread notification count
     * Returns the number of unread notifications for the bell badge.
     *
     * @param projectId Project scope (required)
     * @return Unread count (status code 200)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "getUnreadNotificationCount",
        summary = "Get unread notification count",
        description = "Returns the number of unread notifications for the bell badge.",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Unread count", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = NotificationCountResponse.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = NotificationCountResponse.class))
            }),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProblemDetails.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.GET,
        value = NotificationsApi.PATH_GET_UNREAD_NOTIFICATION_COUNT,
        produces = { "application/json", "application/problem+json" }
    )
    Mono<ResponseEntity<NotificationCountResponse>> getUnreadNotificationCount(
        @NotNull @Parameter(name = "projectId", description = "Project scope", required = true, in = ParameterIn.QUERY) @Valid @RequestParam(value = "projectId", required = true) String projectId,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_LIST_NOTIFICATIONS = "/api/v1/notifications";
    /**
     * GET /api/v1/notifications : List notifications
     * Cursor-based notification list for the authenticated user. Returns notifications for the specified project, ordered by creation time descending. 
     *
     * @param projectId Project scope for notifications (required)
     * @param unreadOnly If true, only return unread notifications (optional, default to false)
     * @param before ISO 8601 cursor — only return notifications created before this timestamp (optional)
     * @param limit Maximum number of notifications to return (capped at 50) (optional, default to 20)
     * @return Paginated notification list (status code 200)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "listNotifications",
        summary = "List notifications",
        description = "Cursor-based notification list for the authenticated user. Returns notifications for the specified project, ordered by creation time descending. ",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Paginated notification list", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = NotificationListResponse.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = NotificationListResponse.class))
            }),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProblemDetails.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.GET,
        value = NotificationsApi.PATH_LIST_NOTIFICATIONS,
        produces = { "application/json", "application/problem+json" }
    )
    Mono<ResponseEntity<NotificationListResponse>> listNotifications(
        @NotNull @Parameter(name = "projectId", description = "Project scope for notifications", required = true, in = ParameterIn.QUERY) @Valid @RequestParam(value = "projectId", required = true) String projectId,
        @Parameter(name = "unreadOnly", description = "If true, only return unread notifications", in = ParameterIn.QUERY) @Valid @RequestParam(value = "unreadOnly", required = false, defaultValue = "false") Boolean unreadOnly,
        @Parameter(name = "before", description = "ISO 8601 cursor — only return notifications created before this timestamp", in = ParameterIn.QUERY) @Valid @RequestParam(value = "before", required = false) @DateTimeFormat(iso = DateTimeFormat.ISO.DATE_TIME) @Nullable OffsetDateTime before,
        @Parameter(name = "limit", description = "Maximum number of notifications to return (capped at 50)", in = ParameterIn.QUERY) @Valid @RequestParam(value = "limit", required = false, defaultValue = "20") Integer limit,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_MARK_ALL_NOTIFICATIONS_READ = "/api/v1/notifications/mark-all-read";
    /**
     * POST /api/v1/notifications/mark-all-read : Mark all notifications as read
     * Marks all unread notifications as read for the authenticated user in the specified project. 
     *
     * @param projectId Project scope (required)
     * @return All notifications marked as read (status code 204)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "markAllNotificationsRead",
        summary = "Mark all notifications as read",
        description = "Marks all unread notifications as read for the authenticated user in the specified project. ",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "204", description = "All notifications marked as read"),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.POST,
        value = NotificationsApi.PATH_MARK_ALL_NOTIFICATIONS_READ,
        produces = { "application/problem+json" }
    )
    Mono<ResponseEntity<Void>> markAllNotificationsRead(
        @NotNull @Parameter(name = "projectId", description = "Project scope", required = true, in = ParameterIn.QUERY) @Valid @RequestParam(value = "projectId", required = true) String projectId,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_MARK_NOTIFICATION_AS_READ = "/api/v1/notifications/{id}/read";
    /**
     * PATCH /api/v1/notifications/{id}/read : Mark a notification as read
     * Marks a single notification as read for the authenticated user.
     *
     * @param id Notification ID (required)
     * @return Notification marked as read (status code 204)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     *         or The requested resource was not found (status code 404)
     */
    @Operation(
        operationId = "markNotificationAsRead",
        summary = "Mark a notification as read",
        description = "Marks a single notification as read for the authenticated user.",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "204", description = "Notification marked as read"),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            }),
            @ApiResponse(responseCode = "404", description = "The requested resource was not found", content = {
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.PATCH,
        value = NotificationsApi.PATH_MARK_NOTIFICATION_AS_READ,
        produces = { "application/problem+json" }
    )
    Mono<ResponseEntity<Void>> markNotificationAsRead(
        @Parameter(name = "id", description = "Notification ID", required = true, in = ParameterIn.PATH) @PathVariable("id") String id,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_UPDATE_NOTIFICATION_PREFERENCES = "/api/v1/notifications/preferences";
    /**
     * PUT /api/v1/notifications/preferences : Update notification preferences
     * Updates the current user&#39;s notification preferences for a project.
     *
     * @param updateNotificationPreferenceRequest  (required)
     * @return Updated preferences (status code 200)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "updateNotificationPreferences",
        summary = "Update notification preferences",
        description = "Updates the current user's notification preferences for a project.",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Updated preferences", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = NotificationPreferenceResponse.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = NotificationPreferenceResponse.class))
            }),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProblemDetails.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.PUT,
        value = NotificationsApi.PATH_UPDATE_NOTIFICATION_PREFERENCES,
        produces = { "application/json", "application/problem+json" },
        consumes = { "application/json" }
    )
    Mono<ResponseEntity<NotificationPreferenceResponse>> updateNotificationPreferences(
        @Parameter(name = "UpdateNotificationPreferenceRequest", description = "", required = true) @Valid @RequestBody Mono<UpdateNotificationPreferenceRequest> updateNotificationPreferenceRequest,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );


    String PATH_UPDATE_PROJECT_NOTIFICATION_SETTINGS = "/api/v1/notifications/project-settings";
    /**
     * PUT /api/v1/notifications/project-settings : Update project notification settings
     * Updates project-level notification settings (admin-level).
     *
     * @param updateProjectNotificationSettingsRequest  (required)
     * @return Updated project settings (status code 200)
     *         or Authentication is required and has failed or has not been provided (status code 401)
     */
    @Operation(
        operationId = "updateProjectNotificationSettings",
        summary = "Update project notification settings",
        description = "Updates project-level notification settings (admin-level).",
        tags = { "Notifications" },
        responses = {
            @ApiResponse(responseCode = "200", description = "Updated project settings", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProjectNotificationSettingsResponse.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProjectNotificationSettingsResponse.class))
            }),
            @ApiResponse(responseCode = "401", description = "Authentication is required and has failed or has not been provided", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ProblemDetails.class)),
                @Content(mediaType = "application/problem+json", schema = @Schema(implementation = ProblemDetails.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.PUT,
        value = NotificationsApi.PATH_UPDATE_PROJECT_NOTIFICATION_SETTINGS,
        produces = { "application/json", "application/problem+json" },
        consumes = { "application/json" }
    )
    Mono<ResponseEntity<ProjectNotificationSettingsResponse>> updateProjectNotificationSettings(
        @Parameter(name = "UpdateProjectNotificationSettingsRequest", description = "", required = true) @Valid @RequestBody Mono<UpdateProjectNotificationSettingsRequest> updateProjectNotificationSettingsRequest,
        @Parameter(hidden = true) final ServerWebExchange exchange
    );

}
