type: object
required: [promptId, promptVersion, requestedBy, targetEnvironment]
properties:
  promptId:
    type: string
  promptVersion:
    type: integer
  requestedBy:
    type: string
  targetEnvironment:
    $ref: '../common/enums/Environment.yaml'
