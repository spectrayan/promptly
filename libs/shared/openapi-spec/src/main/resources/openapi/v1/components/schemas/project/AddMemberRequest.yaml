type: object
required: [userId, role]
properties:
  userId:
    type: string
    description: User ID to add as member
    minLength: 1
    maxLength: 100
    example: usr-002
  role:
    $ref: '../common/enums/ProjectRole.yaml'
