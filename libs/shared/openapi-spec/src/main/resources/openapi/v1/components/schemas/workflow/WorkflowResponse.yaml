type: object
properties:
  id:
    type: string
  promptId:
    type: string
  promptVersion:
    type: integer
  type:
    type: string
    description: "Workflow type (e.g., approval)"
  status:
    $ref: '../common/enums/WorkflowStatus.yaml'
  currentStep:
    type: integer
  requestedBy:
    type: string
  steps:
    type: array
    items:
      $ref: '../workflow/WorkflowStepResponse.yaml'
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
